import { DESCRIPTION_CARDS } from "./descriptionCards";
import { SPECS_CARDS } from "./specsCards";
import { WORKS_CARDS } from "./worksCards";


export const CARDS = [
    { type: 'DESCRIPTION', data: DESCRIPTION_CARDS },
    { type: 'SPECS', data: SPECS_CARDS },
    { type: 'WORKS', data: WORKS_CARDS },
];
